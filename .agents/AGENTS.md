# Custom Workspace Rules

- **Git Commit & Push**: Always stage (`git add .`), commit with a descriptive message, and push (`git push origin main`) to GitHub immediately after making any code changes.
- **Unsure Recipes/Links**: If you cannot find the correct link or are not sure of the recipe, do not make it up. Ask the user to search for the link and provide it.
